default:
  tags:
    - bookstack-runner

stages:
  - build

variables:
  IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA

build:
  stage: build
  script:
    - echo "coucou"
    - docker build -t $IMAGE .
