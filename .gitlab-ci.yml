default:
  tags:
    - bs-runner-3

stages:
  # - quality
  # - test
  - build
  - deploy

variables:
  IMAGE: "$CI_REGISTRY_IMAGE:latest"
  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar" # Defines the location of the analysis task cache
  GIT_DEPTH: "0" # Tells git to fetch all the branches of the project, required by the analysis task
#   PLATFORMS: linux/amd64,linux/arm64

# test-units:
#   stage: test
#   image: php:8.2
#   variables:
#     # Utilisation de SQLite en mémoire pour la rapidité et l'isolation
#     DB_CONNECTION: sqlite
#     DB_DATABASE: ":memory:"
#   before_script:
#     - apt-get update && apt-get install -y libsqlite3-dev # Installation des dépendances nécessaires
#     - composer install --prefer-dist --no-progress
#     - php artisan migrate # Prépare la base de données SQLite en mémoire
#   script:
#     - php artisan test
#   allow_failure: true

build:
  stage: build
  image: docker:24
  services:
    - name: docker:24-dind
  variables:
    DOCKER_TLS_CERTDIR: ""
  script:
    - echo "🔐 Login registry"
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"

    - echo "📦 Pull cache image (if exists)"
    - docker pull "$IMAGE" || true

    - echo "🏗️ Build image with cache"
    - docker build --cache-from "$IMAGE" -t "$IMAGE" -f dev/docker/Dockerfile .

    - echo "⬆️ Push image"
    - docker push "$IMAGE"

deploy:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client
    - eval "$(ssh-agent -s)"
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H "$SWARM_MANAGER_IP" >> ~/.ssh/known_hosts

  script:
    - echo "📤 Upload stack file"
    - scp docker-stack.yml ubuntu@$SWARM_MANAGER_IP:/home/ubuntu/docker-stack.yml

    - echo "♻️ Force update service to pull new image"
    - ssh ubuntu@$SWARM_MANAGER_IP "docker service update --with-registry-auth --force bookstack_app || true"

    - echo "🚀 Deploy stack"
    - ssh ubuntu@$SWARM_MANAGER_IP "docker stack deploy --with-registry-auth -c /home/ubuntu/docker-stack.yml bookstack"

    - echo "📋 Services"
    - ssh ubuntu@$SWARM_MANAGER_IP "docker service ls"

    - echo "📦 Stack status"
    - ssh ubuntu@$SWARM_MANAGER_IP "docker stack ps bookstack"

# sonarcloud-check:
#   stage: quality
#   image:
#     name: sonarsource/sonar-scanner-cli:latest
#     entrypoint: [""]
#   cache:
#     key: "${CI_JOB_NAME}"
#     paths:
#       - .sonar/cache
#   script:
#       - sonar-scanner
